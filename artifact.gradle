android.applicationVariants.all { variant ->
  def appName = "mData"
  variant.outputs.all {
    def newName

    newName =
        "${appName}_${variant.versionName}_${variant.versionCode}_${variant.buildType.name}.apk"
    outputFileName = newName
  }
}